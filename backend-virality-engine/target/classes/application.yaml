server:
  port: 8093

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/guardrail_db
    username: root
    password: pass123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true